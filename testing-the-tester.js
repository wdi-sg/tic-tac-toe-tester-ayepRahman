var box = ['x', 'x', 'x',
           'x', 'x', 'x',
           'x', 'x', 'x']

var playerInput = []


function restart() {

}

function isGameOver() {
  return false
}

function whoWon() {
  return 0
}

function playTurn(index) {
  var newMove = []

  if(index === box[index]) {
    return false
  }

  //return true
}
